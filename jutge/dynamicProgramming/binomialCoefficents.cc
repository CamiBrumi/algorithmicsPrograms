#include <iostream>
#include <vector>

using ll = long long;
using VE = vector<int>;

int const N = 30;
int const K = 30;
VE v;

int main() {
  v = VE(N, VE(K, -1))
  int n, m;
  while (cin >> n >> k) cout << bc(n, k);
}
